diff --git a/src/solidlsp/language_servers/pyright_server.py b/src/solidlsp/language_servers/pyright_server.py
index 1234567..abcdefg 100644
--- a/src/solidlsp/language_servers/pyright_server.py
+++ b/src/solidlsp/language_servers/pyright_server.py
@@ -34,9 +34,8 @@ class PyrightServer(SolidLanguageServer):
         super().__init__(
             config,
             logger,
             repository_root_path,
-            # Note 1: we can also use `pyright-langserver --stdio` but it requires pyright to be installed with npm
-            # Note 2: we can also use `bpyright-langserver --stdio` if we ever are unhappy with pyright
-            ProcessLaunchInfo(cmd="python -m pyright.langserver --stdio", cwd=repository_root_path),
+            # Using basedpyright-langserver directly (requires basedpyright to be installed and in PATH)
+            ProcessLaunchInfo(cmd="basedpyright-langserver --stdio", cwd=repository_root_path),
             "python",
             solidlsp_settings,
         )